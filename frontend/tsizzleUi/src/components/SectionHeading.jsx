export default function SectionHeading({heading}){

    return(
        <div>
                <header className="text-center text-sm">
                    <h1 className="mt-8 mb-5 px-5 font-medium leading-5 tracking-wide">{heading}</h1>
                </header>
            </div>
    )
}